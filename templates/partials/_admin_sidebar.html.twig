
<div class="container-fluid my-4">
    <div class="row">

        <!-- SIDEBAR -->
        <aside class="col-12 col-md-3 col-lg-2 mb-4 mb-md-0">
            <div class="card shadow-sm h-100">
                <div class="card-body p-0">
                    <nav class="nav flex-column">
                        <span class="nav-link fw-bold text-uppercase text-muted small">
                            Administration
                        </span>

                        <a href="{{ path('admin_dashboard') }}" class="nav-link active">
                            📊 Dashboard
                        </a>

                        <a href="{{ path('admin_routes') }}" class="nav-link">
                            🚗 Trajets
                        </a>

                        <a href="{{ path('admin_avis') }}" class="nav-link">
                            ⭐ Avis
                        </a>

                        <a href="{{ path('admin_credits') }}" class="nav-link">
                            💳 Crédits
                        </a>

                        <a href="{{ path('admin_reservations') }}" class="nav-link">
                            📅 Réservations
                        </a>

                        <a href="{{ path('admin_users') }}" class="nav-link">
                            👥 Utilisateurs
                        </a>
                    </nav>
                </div>
            </div>
        </aside>